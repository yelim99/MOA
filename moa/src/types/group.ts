export type GroupInfo = {
  groupId: string;
  groupName: string;
  groupColor: string;
  groupIcon: string;
};

export type GroupInfoDetail = {
  groupId: string;
  groupName: string;
  groupDescription: string;
  groupColor: string;
  groupIcon: string;
};

export type GroupAddInfo = {
  groupName: string;
  groupDescription: string;
  groupColor: string;
  groupIcon: string;
};
