export type GroupInfo = {
  groupId: number;
  groupName: string;
  groupColor: string;
  groupIcon: string;
};

export type GroupInfoDetail = {
  groupId: number;
  groupName: string;
  groupDescription: string;
  groupColor: string;
  groupIcon: string;
};

export type GroupAddInfo = {
  groupName: string;
  groupDescription: string;
  groupColor: string;
  groupIcon: string;
};
